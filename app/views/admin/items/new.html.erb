<div class="container">
  <div class="row">
    <h5 class="col-2 bg-light text-center">商品新規登録</h5>
  </div>
  
  <div class="row">
    <% if @item.errors.any? %>
      <div id="error_explanation">
        <h5>
          <%= pluralize(@item.errors.count, "error") %> prohibited this obj from being saved:
        </h5>
        <ul>
          <% @item.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
  
  <%= render 'detail', item: @item %>
  
</div>